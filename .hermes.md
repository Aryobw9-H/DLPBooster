# DLPBooster (D:\Claude\DLPBooster) — build lessons

- Stack: Tauri 2, vanilla JS (template `C:\Users\Aryo\Downloads\gemini-code-1789211281892.html`), plan: `C:\Users\Aryo\.hermes\plans\2026-09-12_144911-dlpbooster-tauri.md`.
- Toolchain: rustup stable-x86_64-pc-windows-gnu (minimal profile), NO MSVC. Winlibs MinGW at `D:\mingw64` (gcc 16.2 UCRT). Build command needs PATH: `export PATH="/d/mingw64/bin:$HOME/.cargo/bin:$PATH"`.
- Cargo + dead proxy: global git config has `http.proxy=socks5://127.0.0.1:10808` (dead). `~/.cargo/config.toml` sets `[http] proxy = ""` to force direct. Without it every crates.io fetch fails "Could not resolve host".
- dlltool: rustup GNU toolchain ships dlltool.exe but NO as.exe → dlltool fails "CreateProcess". Fixed by winlibs on PATH (has as.exe). `~/.cargo/bin/dlltool.exe` copy harmless, can delete.
- `[target.x86_64-pc-windows-gnu] dlltool = "..."` in cargo config = INVALID (must be a table) — cargo hard-errors. Don't retry that shape.
- Windows-gnu linker caps DLL export ordinals at 65535; Tauri debug builds export ~91k. Fix (applied): `Cargo.toml` `crate-type = ["staticlib", "rlib"]` (cdylib removed, only needed for mobile). Error signature: "export ordinal too large: 91098".
- cargo build --no-default-features ≈51s warm after first full build; `npm run tauri dev` runs, window opens (verified via process list).
- Payload: build_payload.py zips D:\Claude\ddlock payload → payload.zip (gitignored, stays private), not yet wired.
- STATUS 2026-09-12: tasks 0b–12 DONE. dlp-core workspace crate holds ALL console logic (payload, discovery, fov, kvedit, detect, addons, guard, settings, backup, install) — 41/41 tests green. commands.rs wires 12 Tauri commands. ui/ = template port + i18n fa/en + fonts offline. Release exe 19.2MB + MSI + NSIS + .sig updater artifacts. 6 local commits, NOTHING pushed (Task 13 awaits Aryo).
- WHY dlp-core exists: windows-gnu `cargo test` on the tauri crate → test harness exe lacks the Common-Controls v6 manifest → tao/muda's comctl32 TaskDialogIndirect import = STATUS_ENTRYPOINT_NOT_FOUND. Pure logic must stay out of the tauri crate.
- Tauri CLI hangs FOREVER when its output is piped (`| tail`). Always redirect to a file and poll the file.
- Updater signing: `TAURI_SIGNING_PRIVATE_KEY=$(cat ~/.tauri/dlpbooster.key)` works; `TAURI_SIGNING_PRIVATE_KEY_PATH` did NOT (CLI ignored it). Empty password fine.
- Test-hygiene: Windows `remove_dir_all` fails on read-only files → use backup::rm_ro helper in tests. `payload::extract()` uses shared %TEMP%\DLPBoosterPkg → parallel tests race; use `extract_to(per-test dir)`.
- video.txt is BRACED VDF (keys indented under "video.cfg" {) — merge parser must handle indented keys, not just flat lines.
- Tester code placeholder = DLP-2026 in commands.rs (DLPB_TESTER_CODE). Get real code before release.
- Aryo testing protocol: NO screenshots ever — he tests visuals + real installs himself; sandbox installs via DLPB_NOGUARD=1.
